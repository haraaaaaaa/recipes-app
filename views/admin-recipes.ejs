<%- include("includes/head.ejs") %>
<link rel="stylesheet" href="/css/list.css">
</head>
<body>
    <%- include("includes/navbar.ejs") %>
    <div class="list">
    <h1 class="list__header">Recipes Overview</h1>
    <% if (recipes.length === 0) { %>
     <h2 class="list__header">There Are No Added Recipes.</h1>
    <% } else { %>
        <ul class="list__list">
        <% for(const recipe of recipes) { %>
            <%- include("includes/admin-recipe.ejs", {recipe}) %>
        <% } %>
        </ul>
    <% }%>
    </div>
</body>
</html>